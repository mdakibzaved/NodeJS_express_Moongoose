<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Read Page</title>
</head>
<body>
    <div class="w-full  bg-zinc-900 p-10">
        <div class="nav mb-10">
            <a class="text-blue-700 bg-white px-3 py-2 rounded-lg" href="/">Back to Home</a>
        </div>
        <h1 class="text-white  mb-3   tracking-tighter">All Users</h1>
        <div class="users flex gap-4 flex-wrap">
            <% users.forEach((user)=>{ %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-52 bg-red-400 overflow-hidden">
                        <img class="w-full" src="<%= user.image %>" alt="" srcset="">
                    </div>
                    <h3 class=" text-white mt-3 tracking-tighter"> <%=user.name%> </h3>
                    <h5 class="text-zinc-500"> <%= user.email %> </h5>
                    <div class="flex-gap-3 mt-3">
                        <a class="text-zinc-600" href="/edit/<%=user._id %>" >Edit User</a>
                        <a class="text-red-500 ml-2" href="/delete/<%=user._id %>"  >Delete User</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    
</body>
</html>